import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF1a1a1a);
  static const Color cardBackground = Color(0xFF242424);
  static const Color inputBackground = Color(0xFF2a2a2a);
  static const Color surfaceElevated = Color(0xFF303030);
  static const Color surfaceHover = Color(0xFF383838);

  // Borders
  static const Color border = Color(0xFF404040);
  static const Color borderLight = Color(0xFF4a4a4a);

  // Text
  static const Color textPrimary = Color(0xFFffffff);
  static const Color textSecondary = Color(0xFFb3b3b3);
  static const Color textMuted = Color(0xFF8c8c8c);

  // Accent
  static const Color accent = Color(0xFF9b59b6);
  static const Color accentHover = Color(0xFF8e44ad);
  static const Color accentLight = Color(0xFFbb77d6);

  // Status
  static const Color submitted = Color(0xFF9b59b6);
  static const Color assigned = Color(0xFF666666);
  static const Color approved = Color(0xFF4a9b6e);
  static const Color ongoing = Color(0xFFe6a817);

  // Misc
  static const Color error = Color(0xFFe74c3c);
  static const Color white = Color(0xFFffffff);
  static const Color black = Color(0xFF000000);
}
